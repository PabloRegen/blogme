extends ../layouts/layout

block title
	|read post

block content
	h2 #{post.title}
	h3 #{post.subtitle}
	p #{post.body}
	p file != null: #{file != null}

	figure
		img(src=file, alt='Alt post pic', title='Title Post Pic')