extends ../layouts/layout

block title
	|read post

block content
	h2 #{post.title}
	h3 #{post.subtitle}
	p #{post.body}
	figure
		img(src=`/uploads/${post.pic}`, alt='Alt post pic', title='Title Post Pic')